# 專案上傳變更紀錄

## 2026.06.23_11:23:58
* 優化 HyperFrames 圖表動畫：折線圖端點改為穩定淡入與反向退場，移除 hover 造成的 SVG 圓點跳動。
* 重製環狀圖與圓餅圖銜接：以真正扇形 path 取代粗線圈圓餅，轉場初始先貼合環狀圖半徑，再收孔展開成實心圓餅。
* 改善環狀圖中心 HUD：放大數值牌、分離百分比與 AVG LOAD 標籤，避免文字重疊並提升主指標視覺重量。
* 修正雷達圖與氣泡圖切換殘留：雷達點改用本地座標，切換前清除氣泡 opacity 與 transform，避免右下角出現多餘點。
* 強化波形折線圖：改為多段趨勢路徑推進、加入移動取樣點，並在轉入桑基熱流前清除 dash 狀態，讓波形更像即時趨勢訊號。
* 驗證：執行 `node --check hyperframes/app.js`；透過本機瀏覽器驗證折線點 hover 不位移、雷達無殘留點、環狀圖 HUD 不重疊、圓餅轉場貼合環形、波形段取樣點與桑基銜接正常。

## 2026.06.19_23:41:20
* 新增獨立的 Signal Planet 3D 訊息星球頁面，使用本機封裝的 Three.js 建立低多邊形行星、樹木、建築、雲層、訊號塔與軌道膠囊場景。
* 加入拖曳旋轉、慣性、滾輪縮放、訊號塔選取、訊息頻道切換、視角重設與 Web Audio 合成音效。
* 完成訊息發射流程：使用者可輸入新訊息、建立新的頻道項目與 3D 軌道膠囊，並即時更新訊號數與內容面板。
* 從主展覽館導覽列與英雄區加入 3D 星球入口，並完成桌面與行動版 HUD、橫向訊號列表及不重疊的輸入與工具配置。
* 將行星網格、物件密度、材質與內部解析度調整為低階裝置友善設定，近距離桌面及 390×844 行動版實測皆維持 60 FPS。
* 驗證：執行 `node --check messenger/app.js` 與 `git diff --check`；確認主頁與 `/messenger/` HTTP 200，並透過瀏覽器驗證訊號切換、訊息發射、桌面／行動版畫面、無水平溢位與無 console error。

## 2026.06.18_18:30:03
* 將 Math Curve Loaders 擴充為 24 種曲線、四頁各六張的水平分頁收藏庫，加入左右切換、頁碼、響應式控制排列與曲線直連參數。
* 改善曲線展區效能：移除逐粒子陰影模糊、降低主畫布填充解析度與曲線取樣量，僅讓目前選取的縮圖持續動畫，其餘縮圖保留靜態預覽。
* 依原始專案公式校正 Spiral Search，使用四圈角度與餘弦調變半徑，並停用額外整體旋轉。
* 依原始公式重製 Heart Wave 的根號包絡與水平波紋，新增 Stroke、b、Root span、Wave amp、X scale、Y scale 六項即時控制與重設功能。
* 更新 Heart Wave 預設為 Stroke 6.00、b 12.00、Root span 2.20、Wave amp 0.90、X/Y scale 14.00，並提高淺灰底層路徑粗細以改善前後景比例。
* 驗證：執行 `node --check app.js` 與 `git diff --check`；透過本機瀏覽器確認四頁曲線切換、參數即時更新、公式同步、60 FPS 與無 console error。

## 2026.06.18_10:17:14
* 建立網頁技術展覽館，整合粒子英雄場景、WebGL shader、Web Audio 頻譜、拖曳物理節點、資料視覺化與瀏覽器 API 互動展示。
* 重製 Scroll Reactor 為具透視投影、深度排序、慣性拖曳與捲動回饋的 Canvas 3D 反應器，並加入時間軸項目的進場、焦點與退場色彩狀態。
* 加入 Math Curve Loaders 展區與原始靈感連結，提供六種數學曲線、速度、粒子數與尾跡長度控制。
* 改善動畫效能：限制高 DPI 畫布渲染倍率、依可見區域暫停昂貴繪製，並降低非焦點曲線預覽的更新頻率。
* 補上桌面與行動版響應式配置，並將本機視覺驗收截圖排除於版本控制。
* 驗證：執行 `node --check app.js`，並透過本機 HTTP server 確認頁面回應 200；完成桌面、行動版及主要互動區域的截圖檢查。
